/**
 * 再生成コード — カテゴリ: zero_extension
 * 公理数: 6
 * このファイルは rei-regenerate により自動生成されました
 */

// [再生成:semi-fidelity] lim f(n)=L — 縮小収束 [reduce]
function reduction<T>(items: T[], init: T, fn: (a: T, b: T) => T): T {
  return items.reduce(fn, init);
}

// [再生成:semi-fidelity] lim f(n)=L — 縮小収束 [count]
function reduction<T>(items: T[], init: T, fn: (a: T, b: T) => T): T {
  return items.reduce(fn, init);
}

// [再生成:semi-fidelity] lim f(n)=L — 縮小収束 [sum]
function reduction<T>(items: T[], init: T, fn: (a: T, b: T) => T): T {
  return items.reduce(fn, init);
}

// [再生成:semi-fidelity] lim f(n)=L — 縮小収束 [fold]
function reduction<T>(items: T[], init: T, fn: (a: T, b: T) => T): T {
  return items.reduce(fn, init);
}

// [再生成:semi-fidelity] lim f(n)=L — 縮小収束 [accumulate]
function reduction<T>(items: T[], init: T, fn: (a: T, b: T) => T): T {
  return items.reduce(fn, init);
}

// [再生成:semi-fidelity] lim f(n)=L — 縮小収束 [aggregate]
function reduction<T>(items: T[], init: T, fn: (a: T, b: T) => T): T {
  return items.reduce(fn, init);
}
