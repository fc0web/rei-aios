/**
 * 再生成コード — カテゴリ: unified
 * 公理数: 13
 * このファイルは rei-regenerate により自動生成されました
 */

// [再生成:semi-fidelity] f∘g∘h — 合成パイプライン [Promise]
function composition<A, B>(value: A, fn: (a: A) => B): B {
  return fn(value);
}

// [再生成:semi-fidelity] f∘g∘h — 合成パイプライン [await]
function composition<A, B>(value: A, fn: (a: A) => B): B {
  return fn(value);
}

// [再生成:semi-fidelity] f∘g∘h — 合成パイプライン [async]
function composition<A, B>(value: A, fn: (a: A) => B): B {
  return fn(value);
}

// [再生成:semi-fidelity] M₁ → M₂ — モジュール依存 [export]
export const module_4cd6ec4c = {} as const;

// [再生成:semi-fidelity] f∘g∘h — 合成パイプライン [then]
function composition<A, B>(value: A, fn: (a: A) => B): B {
  return fn(value);
}

// [再生成:semi-fidelity] M₁ → M₂ — モジュール依存 [import]
export const module_469e8c5b = {} as const;

// [再生成:semi-fidelity] M₁ → M₂ — モジュール依存 [from]
export const module_003017aa = {} as const;

// [再生成:semi-fidelity] M₁ → M₂ — モジュール依存 [default]
export const module_5c13d641 = {} as const;

// [再生成:semi-fidelity] f∘g∘h — 合成パイプライン [compose]
function composition<A, B>(value: A, fn: (a: A) => B): B {
  return fn(value);
}

// [再生成:semi-fidelity] M₁ → M₂ — モジュール依存 [require]
export const module_414f0165 = {} as const;

// [再生成:semi-fidelity] M₁ → M₂ — モジュール依存 [module]
export const module_3fb45994 = {} as const;

// [再生成:semi-fidelity] f∘g∘h — 合成パイプライン [pipe]
function composition<A, B>(value: A, fn: (a: A) => B): B {
  return fn(value);
}

// [再生成:semi-fidelity] f∘g∘h — 合成パイプライン [chain]
function composition<A, B>(value: A, fn: (a: A) => B): B {
  return fn(value);
}
